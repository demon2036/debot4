# DeBot4 工程协作规范

本文件作用于 `/home/john/debot4` 整个目录树。所有开发、重构、测试、审查和回复都必须遵守。

## 0. 用户目标与推理预算优先

- 每一个 turn 都必须从第一性原理重新确认用户真正要达成的结果、必要证据、现实约束和完成条件，不能只按表面措辞机械执行。
- 在当前 turn 可用的推理、上下文和工具预算内必须充分思考并持续推进，优先把精力用于真正解决用户问题；不得为了节省 juice、缩短回复或快速交差而过早停止。
- 工程仪式不能取代用户结果。搜索、实现、验证和回复都只保留对目标有实际价值的工作，避免重复检查、无关扩展和形式化堆砌。
- 非必要不新增或运行 smoke test。只有它是验证当前风险的最小有效手段，或用户明确要求时才使用；代码变更仍必须运行直接相关的测试，高风险边界仍须有针对性回归验证。
- 非必要不计算或复核 hash。只有完整性、来源追踪、防篡改验证确实依赖 hash，或用户明确要求时才执行。
- 深度思考应转化为可核验的结论、证据和行动，不向用户倾倒冗长的内部推理过程，也不以长篇回复冒充问题已经解决。

## 1. 核心设计原则

- 必须高内聚、低耦合。
- 一个模块只承担一个清晰职责；不要把采集、叙事判断、市场验证、交易执行和展示混在一起。
- 模块之间通过小而明确的接口、不可变数据对象或协议通信。
- 禁止依赖其他模块的内部实现、私有状态或数据库细节。
- 禁止循环依赖、隐式全局状态和跨层快捷调用。
- 外部平台、模型、数据库和网络请求必须封装在独立适配器中，业务规则不得绑定具体供应商。
- 相同规则只能有一个权威实现；不要复制判断逻辑。
- 优先删除死代码和废弃路径，不为旧架构保留无意义兼容层。
- 代码必须精简、有力、可验证；避免空泛抽象、万能类、超长函数和无价值包装。

## 2. Python 代码 300 行硬限制

- 仓库内任何 `.py` 文件，包括源代码和测试，最多 300 个物理行。
- 该限制包含注释、docstring 和空行，不允许通过压缩排版规避。
- 文件接近 250 行时就应主动规划拆分。
- 文件一旦超过 300 行，必须立即停止继续堆代码并完成重构。
- 超过 300 行的代码不得作为完成结果交付。
- 拆分必须按职责边界进行，不能机械地把代码切进无意义的 `utils.py`、`helpers.py` 或 `common.py`。
- 每次涉及 Python 代码的变更后都要检查：

```bash
find . -type f -name '*.py' -not -path './.venv/*' -print0 \
  | xargs -0 -n1 sh -c 'n=$(wc -l < "$0"); [ "$n" -le 300 ] || echo "$n $0"'
```

命令有任何输出，都表示工作尚未完成。

## 3. 模块职责与依赖方向

推荐依赖方向：

```text
外部数据源/API
└── adapters：只负责请求、解析、重试和原始证据
    └── domain：不可变领域对象与纯规则
        └── application：编排用例、队列和状态转换
            ├── persistence：SQLite/文件持久化
            ├── execution：报价、纸面交易与风险边界
            └── presentation：CLI、状态接口和看板
```

- `domain` 不得导入网络、数据库、CLI 或看板代码。
- `presentation` 不得重新实现业务判断。
- `adapters` 不得决定是否买入。
- 叙事研究、交易门禁和执行必须是可独立测试的边界。
- 时间、网络、模型和数据库依赖应可注入，避免测试依赖真实外部状态。

## 4. 变更纪律

- 修改前先定位权威实现和调用方，避免拍脑袋新增第二套逻辑。
- 不需要兼容旧实现时，直接完成清晰重构并删除被替代代码。
- 不得静默吞掉错误、伪造行情、补造证据或把缺失数据当作通过。
- 没有真实验证数据时，不得声称系统有效、可盈利或已经跑通。
- 凭证只能放在仓库外的私有文件或运行环境中，禁止进入源码、配置仓库、日志、提示词和数据库。
- 每次代码变更至少运行与改动直接相关的测试；高风险边界需要补充回归测试。

## 5. Tree 图沟通规范

凡是涉及代码变更，最终回复必须提供本次变更的 tree 图，标明文件职责和调用关系。例如：

```text
debot4/
├── src/debot4/source.py       # 采集外部信号
├── src/debot4/decision.py     # 纯买入门禁
└── tests/
    ├── test_source.py         # 数据源回归
    └── test_decision.py       # 决策边界回归
```

如果运行流程发生变化，还必须给出数据流 tree：

```text
信号到达
├── 写入不可变原始证据
├── 叙事调查
│   ├── 验证最早来源
│   └── 验证 exact CA 与竞争 CA
├── 交易门禁
│   ├── PASS -> 获取决策时实时 MC/报价
│   └── WAIT/REJECT -> 不买
└── 结果追踪
    ├── buy MC
    └── 买入后 1h 最高 MC
```

- 解释回复应尽量优先使用简短 tree 图并在节点后直接写注释。
- tree 图必须反映真实代码和真实流程，不能只作装饰。
- tree 图之后只补充必要结论、验证结果和仍存在的风险。
- 避免大段抽象描述；让人可以快速看清“改了哪里、如何流动、为什么这样做”。

## 6. 完成标准

```text
完成
├── 职责边界清晰，高内聚低耦合
├── 所有 Python 文件 <= 300 行
├── 无新增死代码和重复权威逻辑
├── 相关测试通过
├── 真实运行结果与失败如实记录
├── 凭证未泄露
└── 回复包含准确的变更 tree 与验证结论
```

任一节点未满足，都不能宣称任务已经完成。
